<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="menu.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dashboardPages="clr-namespace:menu.Views.Dashboard"
    xmlns:local="clr-namespace:menu"
    Shell.TabBarIsVisible="False">

    <!--  ShellContent        Title="Home"        ContentTemplate="{DataTemplate local:MainPage}"        Route="MainPage" />  -->
    <!--  ShellContent          Title="Home"        ContentTemplate="{DataTemplate local:MainPage}"        Route="MainPage" />  -->
    <!--  ShellContent          Title="NewPage"        ContentTemplate="{DataTemplate local:NewPage1}"        Route="NewPage" />  -->

    <Shell.FlyoutFooter>
        <StackLayout Padding="20">
            <Button Command="{Binding SettingCommand}" Text="Settings" />
        </StackLayout>
    </Shell.FlyoutFooter>

</Shell>
